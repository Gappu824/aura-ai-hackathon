# The new, modern web framework
fastapi==0.111.0
uvicorn==0.30.1

# Pin Boto3/Botocore for Bedrock support
boto3
python-dotenv==1.0.0
botocore==1.31.57

# The Mangum adapter (still needed, but works better with FastAPI)
mangum==0.17.0

# Other dependencies
sentence-transformers==2.7.0
torch